# Build de produção da Web (usado pelo cliente Desktop/Electron e por qualquer deploy).
# Sob app:// (Electron) não há proxy /api do Vite, por isso o cliente precisa do URL
# absoluto da API. Para um deploy web real, troca este valor pelo URL da API publicada.
VITE_API_URL=http://localhost:3333
